# 📦 Инструкция по установке NBA Value Analyzer v2

## Системные требования

- Python 3.9+
- 2GB RAM
- 500MB свободного места

## Пошаговая установка

### 1. Скачайте проект

```bash
# Распакуйте архив в любую папку
cd /path/to/nba_analyzer_v2
```

### 2. Создайте виртуальное окружение (рекомендуется)

```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

**Если возникают ошибки:**

```bash
# Обновите pip
pip install --upgrade pip

# Установите по одной
pip install flask
pip install nba_api
pip install beautifulsoup4
pip install requests
pip install numpy
pip install pandas
```

### 4. Создайте директории

```bash
mkdir -p templates static cache
```

### 5. Проверьте установку

```bash
python -c "import flask; import nba_api; print('✓ OK')"
```

## Запуск

```bash
python app.py
```

Откройте браузер: **http://localhost:5000**

## Первый запуск

1. Нажмите кнопку **"📊 DEMO"**
2. Должны загрузиться демо данные
3. Если всё работает - отлично!

## Troubleshooting

### Ошибка: `ModuleNotFoundError: No module named 'flask'`

**Решение:**
```bash
pip install flask
```

### Ошибка: `nba_api не найден`

**Решение:**
```bash
pip install nba_api
```

### Ошибка: `Port 5000 already in use`

**Решение:**
```bash
# Измените порт в app.py
app.run(debug=True, port=5001)  # вместо 5000
```

### Кеш не работает

**Решение:**
```bash
# Создайте директорию вручную
mkdir cache
chmod 777 cache  # Linux/Mac
```

### Медленная загрузка

**Причина:** NBA API имеет rate limits

**Решение:**
- Используйте кеш (данные загружаются 1 раз)
- Подождите 1-2 минуты при первой загрузке
- Увеличьте задержки в `data_scraper.py`:
  ```python
  self.request_delay = 1.0  # вместо 0.6
  ```

## Настройка (опционально)

### Изменить TTL кеша

Откройте `cache_system.py`:

```python
self.ttl = {
    'games': timedelta(hours=12),  # вместо 6
    'stats': timedelta(hours=24),  # вместо 12
    # ...
}
```

### Отключить debug mode

Откройте `app.py`:

```python
app.run(debug=False, port=5000)  # debug=False
```

## Обновление

```bash
# Скачайте новую версию
# Сохраните ваш cache/
cp -r cache/ cache_backup/

# Замените файлы
# ...

# Восстановите кеш
cp -r cache_backup/* cache/
```

## Удаление

```bash
# Деактивируйте venv
deactivate

# Удалите папку
rm -rf /path/to/nba_analyzer_v2
```

---

**Нужна помощь?** Создайте issue на GitHub
